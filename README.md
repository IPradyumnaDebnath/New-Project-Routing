clone this repo ###npm install Install all dependencies

npm start
Launch the project

Open http://localhost:3000 to view it in the browser.

*** API URL needs to be adder properly as correct URL is not provided in Task
